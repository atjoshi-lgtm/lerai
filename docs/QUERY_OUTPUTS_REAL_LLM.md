# LeRoy Overrides Writer - Real LLM Extraction Results

This report captures outputs generated by running all queries through **REAL Azure LLM extraction** (not mocked).

- Query fixtures: `tests/fixtures/leroy_overrides_writer_query_cases.json` (12 queries)
- Conflict fixtures: `tests/fixtures/leroy_overrides_writer_conflict_cases.json` (8 queries)
- Current TOML: `tests/fixtures/override.toml` (used for conflict tests only)

## From leroy_overrides_writer_query_cases.json (No Override Fixture)

### 1. Query

Remove map mm2 from Germany. Ticket is LEROYOPS-61.

**Real LLM Intent:**
```json
{
  "Ticket-id": "LEROYOPS-61",
  "Mapnames": [
    "mm2"
  ],
  "Geographical-Scope": {
    "Region-country": [
      "DE"
    ]
  },
  "Override-Directive": {
    "Access-control": "must-exclude"
  }
}
```

**Output:**
````text
✅ **Override Stanza Generated Successfully**

Please review the generated configuration below:

```toml
[[override-records]]
Ticket-id = "LEROYOPS-61"
Mapnames = ["mm2"]
Region-country = ["DE"]
Access-control = "must-exclude"

```

*If this looks correct, you may copy and paste it into `override.toml`.*
````

### 2. Query

Multiply the traffic for map us by 2 across North America.

**Real LLM Intent:**
```json
{
  "Geographical-Scope": {
    "Region-geo": [
      "NA"
    ]
  },
  "Override-Directive": {
    "Quota-multiplier": 2
  },
  "Mapnames": [
    "map us"
  ]
}
```

**Output:**
````text
✅ **Override Stanza Generated Successfully**

Please review the generated configuration below:

```toml
[[override-records]]
Region-geo = ["NA"]
Quota-multiplier = 2

```

*If this looks correct, you may copy and paste it into `override.toml`.*
````

### 3. Query

Set quota percentage of mch-ff-lo to 90% in region 50565.

**Real LLM Intent:**
```json
{
  "Mapnames": [
    "mch-ff-lo"
  ],
  "Geographical-Scope": {
    "Region-number": [
      50565
    ]
  },
  "Override-Directive": {
    "Quota-pct": 90
  }
}
```

**Output:**
````text
✅ **Override Stanza Generated Successfully**

Please review the generated configuration below:

```toml
[[override-records]]
Mapnames = ["mch-ff-lo"]
Region-number = [50565]
Quota-pct = 90

```

*If this looks correct, you may copy and paste it into `override.toml`.*
````

### 4. Query

Allow mm1 to be served in the New York metro area

**Real LLM Intent:**
```json
{
  "Mapnames": [
    "mm1"
  ],
  "Geographical-Scope": {
    "Region-metro": [
      "New_York"
    ]
  },
  "Override-Directive": {
    "Access-control": "allowed"
  }
}
```

**Output:**
````text
✅ **Override Stanza Generated Successfully**

Please review the generated configuration below:

```toml
[[override-records]]
Mapnames = ["mm1"]
Region-metro = ["New_York"]
Access-control = "allowed"

```

*If this looks correct, you may copy and paste it into `override.toml`.*
````

### 5. Query

Set the object count quota for mch-ff-so to 5% in all large regions by default

**Real LLM Intent:**
```json
{
  "Geographical-Scope": {
    "Region-default": [
      "default"
    ]
  },
  "Override-Directive": {
    "Quota-pct": 5
  },
  "Mapnames": [
    "mch-ff-so"
  ]
}
```

**Output:**
````text
✅ **Override Stanza Generated Successfully**

Please review the generated configuration below:

```toml
[[override-records]]
Mapnames = ["mch-ff-so"]
Region-default = ["default"]
Quota-pct = 5

```

*If this looks correct, you may copy and paste it into `override.toml`.*
````

### 6. Query

Multiply the traffic for the us map by 2 across North America.

**Real LLM Intent:**
```json
{
  "Geographical-Scope": {
    "Region-geo": [
      "NA"
    ]
  },
  "Override-Directive": {
    "Quota-multiplier": 2
  }
}
```

**Output:**
````text
✅ **Override Stanza Generated Successfully**

Please review the generated configuration below:

```toml
[[override-records]]
Region-geo = ["NA"]
Quota-multiplier = 2

```

*If this looks correct, you may copy and paste it into `override.toml`.*
````

### 7. Query

Remove mm2 from Germany and France.

**Real LLM Intent:**
```json
{
  "Geographical-Scope": {
    "Region-country": [
      "DE",
      "FR"
    ]
  },
  "Override-Directive": {
    "Access-control": "must-exclude"
  },
  "Mapnames": [
    "mm2"
  ]
}
```

**Output:**
````text
✅ **Override Stanza Generated Successfully**

Please review the generated configuration below:

```toml
[[override-records]]
Mapnames = ["mm2"]
Region-country = ["DE", "FR"]
Access-control = "must-exclude"

```

*If this looks correct, you may copy and paste it into `override.toml`.*
````

### 8. Query

Set the disk quota for map w25 to 40TB and map mm1 to 80TB in region 49440.

**Real LLM Intent:**
```json
{
  "Mapnames": [
    "w25",
    "mm1"
  ],
  "Geographical-Scope": {
    "Region-number": [
      49440
    ]
  },
  "Override-Directive": {
    "Quota-tb": [
      40,
      80
    ]
  }
}
```

**Output:**
````text
✅ **Override Stanza Generated Successfully**

Please review the generated configuration below:

```toml
[[override-records]]
Mapnames = ["w25", "mm1"]
Region-number = [49440]
Quota-tb = [40, 80]

```

*If this looks correct, you may copy and paste it into `override.toml`.*
````

### 9. Query

Force accept the disk quota changes for cw-d-us, cw-d-us-web, and cw-s-au in the default regions.

**Real LLM Intent:**
```json
{
  "Mapnames": [
    "cw-d-us",
    "cw-d-us-web",
    "cw-s-au"
  ],
  "Geographical-Scope": {
    "Region-default": [
      "default"
    ]
  },
  "Override-Directive": {
    "Quota-tb": []
  }
}
```

**Output:**
````text
✅ **Override Stanza Generated Successfully**

Please review the generated configuration below:

```toml
[[override-records]]
Mapnames = ["cw-d-us", "cw-d-us-web", "cw-s-au"]
Region-default = ["default"]
Access-control = "must-include"

```

*If this looks correct, you may copy and paste it into `override.toml`.*
````

### 10. Query

Freeze sticky maps for w4 and w5 in region 52598.

**Real LLM Intent:**
```json
{
  "Geographical-Scope": {
    "Region-number": [
      52598
    ]
  },
  "Override-Directive": {
    "LR-freeze-sticky-maps": true
  }
}
```

**Output:**
````text
✅ **Override Stanza Generated Successfully**

Please review the generated configuration below:

```toml
[[override-records]]
Region-number = [52598]
LR-freeze-sticky-maps = true

```

*If this looks correct, you may copy and paste it into `override.toml`.*
````

### 11. Query

Acknowledge the lost region 49440.

**Real LLM Intent:**
```json
{
  "Geographical-Scope": {
    "Region-number": [
      49440
    ]
  },
  "Override-Directive": {
    "Access-control": "allowed"
  }
}
```

**Output:**
````text
✅ **Override Stanza Generated Successfully**

Please review the generated configuration below:

```toml
[[override-records]]
Region-number = [49440]
Access-control = "allowed"

```

*If this looks correct, you may copy and paste it into `override.toml`.*
````

### 12. Query

Add a rule to exclude w4, w5, w155, w105, w106, w107, and w108 from default regions.

**Real LLM Intent:**
```json
{
  "Geographical-Scope": {
    "Region-default": [
      "default"
    ]
  },
  "Override-Directive": {
    "Access-control": "must-exclude"
  },
  "Mapnames": [
    "w4",
    "w5",
    "w155",
    "w105",
    "w106",
    "w107",
    "w108"
  ]
}
```

**Output:**
````text
✅ **Override Stanza Generated Successfully**

Please review the generated configuration below:

```toml
[[override-records]]
Mapnames = ["w4", "w5", "w155", "w105", "w106", "w107", "w108"]
Region-default = ["default"]
Access-control = "must-exclude"

```

*If this looks correct, you may copy and paste it into `override.toml`.*
````

## From leroy_overrides_writer_conflict_cases.json (With Override Fixture)

### 1. Query

Make sure w5 is excluded from default regions

**Real LLM Intent:**
```json
{
  "Geographical-Scope": {
    "Region-default": [
      "default"
    ]
  },
  "Override-Directive": {
    "Access-control": "must-exclude"
  },
  "Mapnames": [
    "w5"
  ]
}
```

**Output:**
````text
⚠️ **Conflict Detected** ⚠️

Conflict detected! Your request overlaps exactly with existing records from ticket(s): LEROYOPS-1 LEROYOPS-39. You must manually remove the old stanzas before adding this new one.

Please remove the conflicting stanza from `override.toml` before proceeding.
````

### 2. Query

Add a rule to exclude w4, w5, w155, w105, w106, w107, and w108 from default regions.

**Real LLM Intent:**
```json
{
  "Geographical-Scope": {
    "Region-default": [
      "default"
    ]
  },
  "Override-Directive": {
    "Access-control": "must-exclude"
  },
  "Mapnames": [
    "w4",
    "w5",
    "w155",
    "w105",
    "w106",
    "w107",
    "w108"
  ]
}
```

**Output:**
````text
⚠️ **Conflict Detected** ⚠️

Conflict detected! Your request overlaps exactly with existing records from ticket(s): LEROYOPS-1 LEROYOPS-39. You must manually remove the old stanzas before adding this new one.

Please remove the conflicting stanza from `override.toml` before proceeding.
````

### 3. Query

Make sure w5 is excluded from default regions.

**Real LLM Intent:**
```json
{
  "Geographical-Scope": {
    "Region-default": [
      "default"
    ]
  },
  "Override-Directive": {
    "Access-control": "must-exclude"
  },
  "Mapnames": [
    "w5"
  ]
}
```

**Output:**
````text
⚠️ **Conflict Detected** ⚠️

Conflict detected! Your request overlaps exactly with existing records from ticket(s): LEROYOPS-1 LEROYOPS-39. You must manually remove the old stanzas before adding this new one.

Please remove the conflicting stanza from `override.toml` before proceeding.
````

### 4. Query

Remove mm2 from the US.

**Real LLM Intent:**
```json
{
  "Geographical-Scope": {
    "Region-country": [
      "US"
    ]
  },
  "Override-Directive": {
    "Access-control": "must-exclude"
  },
  "Mapnames": [
    "mm2"
  ]
}
```

**Output:**
````text
⚠️ **Conflict Detected** ⚠️

Conflict detected! Your request overlaps exactly with existing records from ticket(s): LEROYOPS-55. You must manually remove the old stanzas before adding this new one.

Please remove the conflicting stanza from `override.toml` before proceeding.
````

### 5. Query

Remove mm2 from the US.

**Real LLM Intent:**
```json
{
  "Geographical-Scope": {
    "Region-country": [
      "US"
    ]
  },
  "Override-Directive": {
    "Access-control": "must-exclude"
  },
  "Mapnames": [
    "mm2"
  ]
}
```

**Output:**
````text
⚠️ **Conflict Detected** ⚠️

Conflict detected! Your request overlaps exactly with existing records from ticket(s): LEROYOPS-55. You must manually remove the old stanzas before adding this new one.

Please remove the conflicting stanza from `override.toml` before proceeding.
````

### 6. Query

Set the quota percentage of mch-ff-lo to 95% in region 50565.

**Real LLM Intent:**
```json
{
  "Mapnames": [
    "mch-ff-lo"
  ],
  "Geographical-Scope": {
    "Region-number": [
      50565
    ]
  },
  "Override-Directive": {
    "Quota-pct": 95
  }
}
```

**Output:**
````text
❌ **Error generating override:**

`Generated TOML violates strict schema: 95 is greater than the maximum of 92`

Please check your input and try again.
````

### 7. Query

Allow map brand_new_map in region 59999

**Real LLM Intent:**
```json
{
  "Mapnames": [
    "brand_new_map"
  ],
  "Geographical-Scope": {
    "Region-number": [
      59999
    ]
  },
  "Override-Directive": {
    "Access-control": "allowed"
  }
}
```

**Output:**
````text
✅ **Override Stanza Generated Successfully**

Please review the generated configuration below:

```toml
[[override-records]]
Mapnames = ["brand_new_map"]
Region-number = [59999]
Access-control = "allowed"

```

*If this looks correct, you may copy and paste it into `override.toml`.*
````

### 8. Query

Exclude brand_new_map from Germany

**Real LLM Intent:**
```json
{
  "Geographical-Scope": {
    "Region-country": [
      "DE"
    ]
  },
  "Override-Directive": {
    "Access-control": "must-exclude"
  },
  "Mapnames": [
    "brand_new_map"
  ]
}
```

**Output:**
````text
✅ **Override Stanza Generated Successfully**

> **Note:** Note: You are setting a broad Region-country rule. Ensure it doesn't conflict with deeper, region-specific topology overrides.

Please review the generated configuration below:

```toml
[[override-records]]
Mapnames = ["brand_new_map"]
Region-country = ["DE"]
Access-control = "must-exclude"

```

*If this looks correct, you may copy and paste it into `override.toml`.*
````
